class String {
  func String() {
    this.length = 0;
  }

  func concat(newStr) {
    for (set i; i < newStr.length; i += 1) {
      this[i + this.length] = newStr[i];
    }

    this.length += newStr.length;

    return this;
  }
}

return String;